from textnode import TextNode


# create a main() function that creates a new TextNode object with some dummy values. Print the object and make sure it looks like the expected output.
def main():
    text_node = TextNode("Hello, World!", "text", "https://www.example.com")
    print(text_node)
